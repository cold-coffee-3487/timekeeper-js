# timekeeper-js

A very very lean app using pure Javascript.

Enter a start time, a stop time, and compute the elapsed time between those.

Each pair of start/stop times will be added to a table.

Then, compute a total elapsed time using each row in a table.

Runs a very basic nodejs server.